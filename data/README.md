# Data Directory

This directory is for storing datasets used in learning sessions.

## Structure

- `raw/` - Raw, unprocessed data
- `processed/` - Processed and cleaned data
- `models/` - Saved model files (gitignored)

## Usage

Most examples in this project use synthetic data generated on-the-fly. For real datasets, place them in the appropriate subdirectory.
